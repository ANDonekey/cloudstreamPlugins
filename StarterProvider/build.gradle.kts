// Use an integer for version numbers
version = 1

cloudstream {
    description = "Starter template for a new Cloudstream provider"
    authors = listOf("ANDonekey")
    status = 3

    tvTypes = listOf("Movie", "TvSeries")
    language = "zh"
    requiresResources = false
}

android {
    namespace = "com.starter"
}
