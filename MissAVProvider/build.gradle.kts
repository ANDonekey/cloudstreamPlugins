version = 1

cloudstream {
    description = "MissAV.ws provider for Cloudstream (Chinese categories)"
    authors = listOf("ANDonekey", "Codex")
    status = 3

    tvTypes = listOf("NSFW")
    language = "zh"
    requiresResources = false
    iconUrl = "https://missav.ws/favicon.ico"
}

android {
    namespace = "com.missav"
}

